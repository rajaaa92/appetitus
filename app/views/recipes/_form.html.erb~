<%= form_for(@recipe) do |f| %>
  <% if @recipe.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@recipe.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @recipe.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <br> Name: <br> <%= f.text_field :name %>
  </p>
  <div class="row">
		<div class="span12">
			<div class="row">
				<div class="span6">
					<p>
    				Ingredients: <br>
    				<%= f.text_area :ingredients %>
					</p>
				</div>
				<div class="span6">
					<p>
						Content: <br>
						<%= f.text_area :content %>
					</p>
				</div>
			</div>
		</div>
  </div>
	<p>
		rate <br> <%= f.text_field :rate %>
	</p>
	<p>
    Added by: <br> <%= f.text_field :user_id %>
  </p>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
