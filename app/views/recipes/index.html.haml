.well.well-lg
  %h2 Recipes
  %table.table.table-condensed
    %tr
      %th Name
      %th Added by
      %th Likes
    - recipes.each do |r|
      %tr
        %td= link_to r.name, r
        %td= User.find(r.user_id).email[/(.*@)/].chop
        %td= r.rate
