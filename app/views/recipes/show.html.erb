<h2> <%=h @recipe.name %> </h2> 

<hr>

<h3> <b> Ingredients: </b>  </h3> <br>
<%=h simple_format(@recipe.ingredients) %>
<br>
<h3> <b> Content: </b> </h3> <br>
<%=h simple_format(@recipe.content) %>

<hr>

<div class="row">
	<div class="span1">
		<b> Added by: </b> <br>
		<b> Likes: </b>
	</div>
	<div class="span1">
		<% u = User.find(@recipe.user_id).email %>
		<% u =  u[/(.*@)/] %>
		<%=h  u.chop %> <br>
		<%=h @recipe.rate %>
	</div>
	<div class="span1">
		<br>
		<%= link_to 'Like it!', {:action => :like_it, :id => @recipe.id, :remote => true}, {:class=>'btn btn-primary'} %>
	</div>
	<div class="span1">
		<%= link_to 'Add to my cookbook', new_cookbook_path(:recipe_id => @recipe.id), :class => "btn btn-primary" %>
	</div>
</div>

<br>

<%= link_to 'Edit', edit_recipe_path(@recipe) %> |
<%= link_to 'Back', recipes_path %>

<hr>

<div class="row">

	<div class="span6">
		<% if @recipe.comments == [] %>
			<h4> No comments </h4>
		<% else %>
			<h4> Comments </h4> 
		<% end %>

		<br>

		<% @recipe.comments.each do |c| %>
			<table class="table">          
				<thead>
					<tr>
						<div class="well">
							<% t = User.find(c.user_id).email %>
							<% t = t[/(.*@)/] %>
							<b> <%=h  t.chop %> </b> says:
					 		<i> (<%= time_ago_in_words(c.created_at) %> ago)</i>
							<br>
							<%=h c.content %>
						</div>
					</tr>
				</thead>
			</table>
		<% end %>
	</div>

	<div class="span4">
		<%= form_for([@recipe, @recipe.comments.build]) do |f| %>
				<b> Add your comment... </b> <br />
				<div class="field">
					<%= f.text_area :content %>
				</div>
		
			<p>
				<%= f.submit "Add Comment" %>
			</p>
		<% end %>
	</div>

</div>






